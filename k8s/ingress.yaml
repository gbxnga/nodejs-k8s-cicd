apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: my-ingress
spec:
  rules: 
  - host: jenkins.gbengaoni.com
    http:
      paths:
      - path:
        backend:
          serviceName: jenkins-ci
          servicePort: 8080
 
 